/**
 * @winlerr/notifications — multi-channel notification helpers
 *
 * Status: Deferred (Proposal)
 *
 * No legitimate architectural reason to establish a provider-independent
 * notification interface at this phase. Future notifications (email, WhatsApp,
 * SMS, in-app) will be delivered via @winlerr/integrations adapters when
 * a concrete use case is validated.
 *
 * This package remains a placeholder until HQ/Product confirms the first
 * notification channel.
 */

export const placeholder = "notifications" as const;
