/**
 * @winlerr/ui — shared UI components
 *
 * This is a placeholder foundation. Add reusable React components here
 * and export them from this entry point.
 *
 * Example:
 * export { Button } from "./components/button";
 */

export const placeholder = "ui" as const;
