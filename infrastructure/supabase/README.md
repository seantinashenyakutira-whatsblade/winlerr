# Supabase — Winlerr

Migrations live in migrations/ as <timestamp>_<name>.sql.

- Enable RLS on all tenant tables.
- Never mutate prod via dashboard without a migration.
- Store types in packages/database.
